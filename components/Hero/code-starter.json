[
  {
    "language": "typescript",
    "code": "export function formatDate(date: Date): string { return new Intl.DateTimeFormat('en-US', { year: 'numeric', month: 'long', day: 'numeric' }).format(date); }"
  },
  {
    "language": "javascript",
    "code": "export function formatDate(date) { return new Intl.DateTimeFormat('en-US', { year: 'numeric', month: 'long', day: 'numeric' }).format(date); }"
  },
  {
    "language": "rust",
    "code": "use chrono::{DateTime, Local}; pub fn format_date(date: DateTime<Local>) -> String { date.format(\"%B %d, %Y\").to_string() }"
  },
  {
    "language": "postgresql",
    "code": "CREATE OR REPLACE FUNCTION format_date(d DATE) RETURNS TEXT AS $$ BEGIN RETURN TO_CHAR(d, 'FMMonth DD, YYYY'); END; $$ LANGUAGE plpgsql;"
  },
  {
    "language": "c",
    "code": "#include <stdio.h> #include <time.h> void format_date(char *buffer, size_t size) { time_t now = time(NULL); struct tm *t = localtime(&now); strftime(buffer, size, \"%B %d, %Y\", t); }"
  },
  {
    "language": "java",
    "code": "import java.time.LocalDate; import java.time.format.DateTimeFormatter; public class Utils { private static final DateTimeFormatter FORMATTER = DateTimeFormatter.ofPattern(\"MMMM dd, yyyy\"); public static String formatDate(LocalDate date) { return date.format(FORMATTER); } }"
  },
  {
    "language": "python",
    "code": "from datetime import datetime def format_date(date: datetime) -> str: return date.strftime(\"%B %d, %Y\")"
  },
  {
    "language": "go",
    "code": "package utils import \"time\" func FormatDate(t time.Time) string { return t.Format(\"January 02, 2006\") }"
  },
  {
    "language": "csharp",
    "code": "using System; using System.Globalization; public static class Utils { public static string FormatDate(DateTime date) { return date.ToString(\"MMMM dd, yyyy\", CultureInfo.InvariantCulture); } }"
  },
  {
    "language": "kotlin",
    "code": "import java.time.LocalDate import java.time.format.DateTimeFormatter object Utils { private val formatter = DateTimeFormatter.ofPattern(\"MMMM dd, yyyy\") fun formatDate(date: LocalDate): String { return date.format(formatter) } }"
  }
]
